apiVersion: v1
kind: Pod
metadata:
  name: nodehelloworld.example.com
  labels:
    app: helloworld
spec:
  containers:
  - name: latest
    image: yaviram/myrepo
    ports:
    - name: nodejs-port
      containerPort: 3000
